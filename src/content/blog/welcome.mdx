---
title: "Discover Explainer"
description: "Discover Explainer is a tool that helps you create beautiful, responsive, and accessible web documentation using Astro. It's built with Astro, Tailwind CSS, and TypeScript."
permalink: "welcome"
authors:
  - leadcode_dev
# thumbnail: https://github-production-user-asset-6210df.s3.amazonaws.com/904724/261574805-5af726c5-0113-4676-9ee9-c464393caee0.png
publishedAt: 2024-01-01:23:00:00
---

:::code-group labels=[pnpm,,app.dart, Dockerfile, tailwind.config.ts]

```bash
npm install rehype-code-group
```

```rs
pub struct Foo {
  pub name: String,
}

impl Foo {
  pub fn new(name: String) -> Self {
    Self { name }
  }
}
```

```dart
final class Foo {
  final String name;
  Foo({required this.name});
}
```

```yaml
docker:
  image: node:20
  ports:
    - "3000:3000"
  volumes:
    - .:/app
```

```typescript
export const config = {}
```

```sql
CREATE TABLE users (
  id INT PRIMARY KEY,
  name VARCHAR(255) NOT NULL,
  email VARCHAR(255) NOT NULL
);
```
:::

:::code-group labels=[pnpm,,app.dart, Dockerfile]

```bash
npm install rehype-code-group
```

```rs
pub struct Foo {
  pub name: String,
}

impl Foo {
  pub fn new(name: String) -> Self {
    Self { name }
  }
}
```

```dart
final class Foo {
  final String name;
  Foo({required this.name});
}
```

```yaml
docker:
  image: node:20
  ports:
    - "3000:3000"
  volumes:
    - .:/app
```

```typescript
export const config = {}
```

```sql
CREATE TABLE users (
  id INT PRIMARY KEY,
  name VARCHAR(255) NOT NULL,
  email VARCHAR(255) NOT NULL
);
```
:::